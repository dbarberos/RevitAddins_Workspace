# Implementación de Instalador Guiado Local

## 1. Interfaz de Usuario (XAML)
- [ ] Añadir botón `AnalyzeHardwareButton` y panel de diagnóstico.
- [ ] Añadir `ProgressBar` y etiqueta de porcentaje.
- [ ] Añadir botón `InstallButton` (separado del de Render).
- [ ] Configurar estados iniciales (bloqueo de botones).

## 2. Lógica de Detección (C#)
- [ ] Implementar servicio de detección de GPU/VRAM mediante WMI.
- [ ] Definir umbrales de bloqueo (ej: < 8GB RAM total o Fallo de detección).
- [ ] Implementar gestor de estados (Actualizar habilitación de controles).

## 3. Motor de Instalación (Python & C#)
- [ ] Crear script `install_engine.py` para descarga de Python portable y modelos.
- [ ] Implementar comunicación de progreso (stdout -> C# -> ProgressBar).
- [ ] Vincular el botón "Instalar" con el proceso de descarga real.

## 4. Verificación
- [ ] Compilar y probar el flujo completo en el PC del usuario.
- [ ] Verificar que el botón "Renderizar" solo se activa si la instalación fue exitosa.
