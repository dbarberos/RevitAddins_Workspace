# Task - Fix Crash & Debug Window

- [x] Add `IsBulkUpdating` to `TreeItemViewModel.cs`.
- [x] Update `LoggerService.cs` with `ObservableCollection` and `LogInfo`.
- [x] Instrument `SelectionFilterViewModel.cs` with bulk update flag and logging.
- [x] Create `LogViewModel.cs`.
- [x] Create `LogView.xaml` and `LogView.xaml.cs`.
- [x] Update `SelectionFilterView.xaml.cs` to show the log window.
- [x] Fix compilation errors (ambiguous `Application` and `Binding`).
- [x] Verify successful build.
