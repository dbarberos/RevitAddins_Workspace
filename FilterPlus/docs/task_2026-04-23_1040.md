# Task list: Treeview Enhancements 

- [x] Validate and restore `HierarchicalDataTemplate` using explicit `x:Key` and explicit application in `ItemTemplate`.
- [x] Modify ViewModels to support leaf node `Count` and accumulation.
- [x] Remove border elements encompassing the tree view control.
- [x] Build multi-column header (`Elements` and `Count`) directly above the tree view.
- [x] Introduce three-state checkbox support to visually represent partial node check states.
- [x] Modify background colours based on checkbox states (Transparent -> Green -> Yellow).
- [x] Modify structural UI code to extend background color highlighting bounds full-width/height, removing indentation borders.
- [x] Reset SystemColors native Highlight settings in WPF to clear the gray-blue overlapping tint.
- [x] Verify Add-in build success and instruct user to restart Revit application blocking execution contexts.
