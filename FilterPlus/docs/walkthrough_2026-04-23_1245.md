# Walkthrough - Active Global Logic Layouts

## What was built
During the add-in launch, if a structural engineer already has pre-cached `ElementId` instances physically highlighted in standard Revit usage, the hierarchical engine immediately constructs a comprehensive index of EVERYTHING currently active and viewable within the native document. 

Upon complete construction, the logic scans discretely comparing leaf element references to original UI selection caches. When a match applies, the checkbox evaluates True, forcing parent nodes to dynamically compute intermediate categorisations. Paths are visually expanded identically routing exactly the elements pre-selected while exposing unchecked generic categories parallel. 

If an architect clicks the add-in completely bare with a 0 metric layout, it maintains absolute structure collapse parsing exactly the uppermost `All` parent string line alongside native root categories unselected.

Compiled successfully ensuring stable UX functionality completely adhering to intended specifications natively.
