# Phase 2: Filter Input

- [x] Add `IsVisible` property to `TreeItemViewModel`.
- [x] Implement filter state saving (selection snapshot) in `SelectionFilterViewModel`.
- [x] Add `FilterText`, `OnFilterTextChanged`, `ApplySearchFilter()`, and `ClearSearchCommand` in `SelectionFilterViewModel`.
- [x] Add the filter UI row to `SelectionFilterView.xaml` (Title left, Input middle, Button right).
- [x] Bind `TreeViewItem` visibility to `IsVisible` using a converter.
