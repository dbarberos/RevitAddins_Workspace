# Task List: TreeView Bug Fixes

- [x] Integrate iteration depth calculations into `TreeItemViewModel` initializations via `SelectionFilterViewModel.cs`.
- [x] Design inline custom `ControlTemplate` for standard WPF `TreeViewItem` eliminating system indents.
- [x] Configure explicit line bottom margins using white border `BorderThickness="0,0,0,1"`.
- [x] Correct invalid recursion states failing background calculations by guarding `IsChecked` calls via `_isUpdatingState`.
- [x] Programmatically hijack the `IsThreeState` WPF `CheckBox` routing so direct user interactions block intermediate `null` states.
