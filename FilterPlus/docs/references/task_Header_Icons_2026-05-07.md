# Task List: Explorer Header Icons & Logic

- [x] Define `HeaderIconButtonStyle` in `SelectionFilterView.xaml`.
- [x] Add Expand (arrow-down) and Collapse (arrow-up) buttons to header.
- [x] Define `ExpandAll` and `CollapseAll` commands in `SelectionFilterViewModel.cs`.
- [x] Implement Step-by-Step Expansion logic.
- [x] Implement Step-by-Step Collapse logic.
- [x] Implement **Semantic Depth Memory**: Preserve conceptual level across tree rebuilds.
- [x] Fix: Prevent selection-restoration logic from overriding expansion state (`forceExpand` flag).
- [x] Fix: Prevent WPF recycling state corruption (Set `VirtualizationMode` to `Standard`).
- [x] Compile and verify functionality (0 errors).
- [x] Store final documentation in repository.
