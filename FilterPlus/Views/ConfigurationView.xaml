<Window x:Class="FilterPlus.Views.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:FilterPlus.ViewModels"
        Title="Configuration" Height="380" Width="380"
        WindowStartupLocation="CenterScreen"
        Topmost="True" Background="#f5f5f5"
        x:Name="ConfigWindow">
    <Window.DataContext>
        <viewmodels:ConfigurationViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="Auto"/> <!-- Options -->
            <RowDefinition Height="Auto"/> <!-- Contextual Checkbox -->
            <RowDefinition Height="Auto"/> <!-- Note -->
            <RowDefinition Height="*"/>    <!-- Spacer -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tab Option" FontWeight="Bold" FontSize="16" Margin="0,0,0,15" Foreground="#333"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <RadioButton Content="Place FilterPlus on DBDev tab(default)" IsChecked="{Binding IsDBDevSelected}" Margin="0,0,0,10"/>
            <RadioButton Content="Place on Revit default tab" IsChecked="{Binding IsRevitDefaultSelected}" Margin="0,0,0,10"/>
            <RadioButton Content="Place on tab named:" IsChecked="{Binding IsCustomSelected}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding CustomTabName, UpdateSourceTrigger=PropertyChanged}" 
                     IsEnabled="{Binding IsCustomSelected}" 
                     Margin="20,0,0,0" Padding="3" Width="200" HorizontalAlignment="Left" />
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,20">
            <CheckBox Content="Use FilterPlus as Contextual Filter" IsChecked="{Binding UseAsContextualFilter}" VerticalAlignment="Center"/>
            <Button Content=" ? " Command="{Binding ShowHelpDialogCommand}" Margin="10,0,0,0" Width="22" Height="22" 
                    Background="#ddd" BorderThickness="0" Cursor="Hand" ToolTip="Click for details about version support"
                    FontWeight="Bold" Foreground="#333"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="* You must restart Revit in order to apply these changes." 
                   FontStyle="Italic" Foreground="Gray" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" CommandParameter="{Binding ElementName=ConfigWindow}" Width="80" Margin="0,0,10,0" Background="#007ACC" Foreground="White" FontWeight="Bold"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=ConfigWindow}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
