<Window x:Class="FilterPlus.Views.SaveSelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Name="SaveWindow"
        Title="Save Selection" Height="190" Width="480"
        WindowStartupLocation="CenterOwner"
        Topmost="True" Background="#f5f5f5" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="RowButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#e0e0e0"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="IsEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActiveButtonStyle" TargetType="Button" BasedOn="{StaticResource RowButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ROW 1: SAVE NEW -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Save New" 
                    Command="{Binding SaveNewCommand}" 
                    CommandParameter="{Binding ElementName=SaveWindow}"
                    IsEnabled="{Binding IsNewNameValid}"
                    Style="{StaticResource ActiveButtonStyle}"/>
            <TextBox Grid.Column="1" Text="{Binding NewSelectionName, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalContentAlignment="Center" Margin="5" Padding="4" Height="30" BorderBrush="#e0e0e0" BorderThickness="1"/>
        </Grid>

        <!-- ROW 2: OVERWRITE -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Overwrite" 
                    Command="{Binding OverwriteCommand}" 
                    CommandParameter="{Binding ElementName=SaveWindow}"
                    IsEnabled="{Binding IsExistingSelectionSelected}"
                    Style="{StaticResource ActiveButtonStyle}"/>
            <ComboBox Grid.Column="1" ItemsSource="{Binding ExistingSelections}" 
                      SelectedItem="{Binding SelectedExistingSelection}"
                      DisplayMemberPath="Name"
                      VerticalContentAlignment="Center" Margin="5" Height="30" BorderBrush="#e0e0e0" BorderThickness="1"/>
        </Grid>

        <!-- FOOTER: CANCEL -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" CommandParameter="{Binding ElementName=SaveWindow}" Width="80" Height="26" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#e0e0e0" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
