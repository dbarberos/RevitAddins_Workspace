<Window x:Class="TransferPlus.Views.TransferPlusView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TransferPlus.Views"
        xmlns:viewmodels="clr-namespace:TransferPlus.ViewModels"
        mc:Ignorable="d"
        Title="TransferPlus" Height="788"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">

    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Width" Value="810"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRenamePanelOpen}" Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width" To="1310" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Width" To="810" Duration="0:0:0.3">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/TransferPlus;component/Resources/Styles/TransferPlusStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <local:NegativeConverter x:Key="NegativeConverter"/>
            
            <HierarchicalDataTemplate x:Key="CheckboxTreeTemplate" DataType="{x:Type viewmodels:TreeItemViewModel}" ItemsSource="{Binding Children}">
                <Grid Width="{Binding ViewportWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="60"/>
                    </Grid.ColumnDefinitions>
                    
                    <Grid Grid.Column="0" ClipToBounds="True">
                        <StackPanel Orientation="Horizontal" Margin="2,4">
                            <StackPanel.RenderTransform>
                                <TranslateTransform X="{Binding Value, ElementName=TreeHScroll, Converter={StaticResource NegativeConverter}}"/>
                            </StackPanel.RenderTransform>
                            <CheckBox IsChecked="{Binding IsChecked}" IsThreeState="True" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Name}" FontWeight="Normal" Margin="0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                    
                    <TextBlock Grid.Column="1" Text="{Binding Count}" Foreground="LightGray" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                </Grid>
            </HierarchicalDataTemplate>

            <Style x:Key="CardBorderStyle" TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="15,10"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
                <Setter Property="BorderBrush" Value="#e8e8e8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect Color="#000000" Opacity="0.03" BlurRadius="5" ShadowDepth="2"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="FilterPlusTextBlockStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="#333"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>
                <ColumnDefinition.Style>
                    <Style TargetType="ColumnDefinition">
                        <Setter Property="Width" Value="*"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRenamePanelOpen}" Value="True">
                                <Setter Property="Width" Value="Auto"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ColumnDefinition.Style>
            </ColumnDefinition>
            <ColumnDefinition>
                <ColumnDefinition.Style>
                    <Style TargetType="ColumnDefinition">
                        <Setter Property="Width" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRenamePanelOpen}" Value="True">
                                <Setter Property="Width" Value="*"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ColumnDefinition.Style>
            </ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Row 0: Apply filter from -->
                <RowDefinition Height="*"/>    <!-- Row 1: Main Tree + Right Side Cards -->
                <RowDefinition Height="Auto"/> <!-- Row 2: Submit Buttons -->
            </Grid.RowDefinitions>

        <!-- TOP ROW: SOURCE SELECT AND INCLUDE LINKS -->
        <Border Grid.Row="0" Margin="0,0,0,5" Padding="0,0,0,8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Apply transfer from:" FontWeight="Bold" FontSize="12" Margin="0,0,0,5" Foreground="#333"/>
                    <Border BorderBrush="#007ACC" BorderThickness="1.5" CornerRadius="4" Height="30" Background="White" VerticalAlignment="Center">
                        <ComboBox ItemsSource="{Binding SourceDocuments}" SelectedItem="{Binding SelectedSourceDocument}" DisplayMemberPath="Nombre" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="8,0">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding EsVinculo}" Value="True"/>
                                                <Condition Binding="{Binding DataContext.CopyLinks, RelativeSource={RelativeSource AncestorType=ComboBox}}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="IsEnabled" Value="False"/>
                                            <Setter Property="Opacity" Value="0.5"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>
                    </Border>
                </StackPanel>

                <CheckBox Grid.Column="1" Content="Include Links as Source" IsChecked="{Binding CopyLinks}" Style="{StaticResource SwitchStyle}" VerticalAlignment="Bottom" Margin="10,0,9,3"/>
            </Grid>
        </Border>
        
        <!-- MAIN ROW -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="376"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="376"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column: Source Selection, Explorer TreeView, Destination Selection -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Transfer To header -->
                    <RowDefinition Height="0.75*"/> <!-- Transfer To ListBox (reduced to 0.75x) -->
                    <RowDefinition Height="Auto"/> <!-- What header -->
                    <RowDefinition Height="Auto"/> <!-- Tree columns header -->
                    <RowDefinition Height="2*"/>   <!-- TreeView -->
                </Grid.RowDefinitions>

                <!-- Transfer To -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Transfer to:" FontWeight="Bold" FontSize="12" Foreground="#333" Margin="0"/>
                    <TextBlock Text="{Binding CheckedDestinationsText}" FontSize="11" Foreground="#666" Margin="10,1,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <Border Grid.Row="1" BorderBrush="#ddd" BorderThickness="1" Background="White" Margin="0,0,0,10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding DestinationDocuments}" Margin="10">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Nombre}" IsChecked="{Binding Checked, Mode=TwoWay}" Margin="0,4" FontSize="12"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- What Header -->
                <TextBlock Grid.Row="2" Text="What:" FontWeight="Bold" FontSize="12" Margin="0,5,0,5" Foreground="#333"/>

                <!-- TreeView Header Row -->
                <Border Grid.Row="3" Background="#f0f0f0" BorderBrush="#ddd" BorderThickness="1,1,1,0" Padding="5,6">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center" Margin="0">
                            <TextBlock Text="Elements" FontWeight="SemiBold" FontSize="11" Foreground="#555" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <Button Command="{Binding ExpandAllCommand}" Style="{StaticResource HeaderIconButtonStyle}" ToolTip="Expand All" VerticalAlignment="Center">
                                <Path Data="M0,0 L10,0 L5,8 Z" Fill="#666" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>
                            <Button Command="{Binding CollapseAllCommand}" Style="{StaticResource HeaderIconButtonStyle}" ToolTip="Collapse All" VerticalAlignment="Center">
                                <Path Data="M5,0 L10,8 L0,8 Z" Fill="#666" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="Count" FontWeight="SemiBold" FontSize="11" Foreground="#555" HorizontalAlignment="Right" Margin="0,0,10,0"/>
                    </Grid>
                </Border>

                <!-- TreeView + Horizontal ScrollBar container -->
                <Grid Grid.Row="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" BorderBrush="#ddd" BorderThickness="1" Background="White">
                        <TreeView FlowDirection="LeftToRight" 
                                  ItemsSource="{Binding RootNodes}" 
                                  ItemTemplate="{StaticResource CheckboxTreeTemplate}"
                                  VirtualizingStackPanel.IsVirtualizing="True"
                                  VirtualizingStackPanel.VirtualizationMode="Standard"
                                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  BorderThickness="0" Background="Transparent">
                            <TreeView.ItemContainerStyle>
                                <Style TargetType="TreeViewItem">
                                    <Setter Property="Visibility" Value="{Binding IsVisible, Converter={StaticResource BoolToVis}}"/>
                                    <Setter Property="Background" Value="#f9f9f9"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TreeViewItem">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition/>
                                                    </Grid.RowDefinitions>
                                                    <!-- Header row acts as background for whole line. Border is White for gap -->
                                                    <Border Name="Bd" Background="{TemplateBinding Background}" BorderBrush="White" BorderThickness="0,0,0,1" SnapsToDevicePixels="true">
                                                        <Grid>
                                                             <Grid.ColumnDefinitions>
                                                                 <ColumnDefinition Width="Auto"/>
                                                                 <ColumnDefinition Width="*"/>
                                                             </Grid.ColumnDefinitions>
                                                             <!-- Expander Toggle -->
                                                             <ToggleButton x:Name="Expander" Grid.Column="0" Margin="{Binding IndentMargin}" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                                                 <ToggleButton.Template>
                                                                     <ControlTemplate TargetType="ToggleButton">
                                                                         <Border Width="20" Height="20" Background="Transparent" Cursor="Hand">
                                                                             <TextBlock x:Name="ExpanderText" Text="▶" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10" Foreground="Gray"/>
                                                                         </Border>
                                                                         <ControlTemplate.Triggers>
                                                                             <Trigger Property="IsChecked" Value="True">
                                                                                 <Setter TargetName="ExpanderText" Property="Text" Value="▼"/>
                                                                             </Trigger>
                                                                         </ControlTemplate.Triggers>
                                                                     </ControlTemplate>
                                                                 </ToggleButton.Template>
                                                             </ToggleButton>
                                                             <!-- Content Row -->
                                                             <ContentPresenter x:Name="PART_Header" Grid.Column="1" ContentSource="Header" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                                        </Grid>
                                                    </Border>
                                                    <!-- No margin on ItemsHost to avoid WPF default indentation cutting off Backgrounds -->
                                                    <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" />
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsExpanded" Value="false">
                                                        <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                                    </Trigger>
                                                    <Trigger Property="HasItems" Value="false">
                                                        <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsChecked}" Value="True">
                                            <Setter Property="Background" Value="#e8f5e9"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsChecked}" Value="{x:Null}">
                                            <Setter Property="Background" Value="#fff8e1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TreeView.ItemContainerStyle>
                        </TreeView>
                    </Border>
                <ScrollBar Grid.Row="1" Orientation="Horizontal" Name="TreeHScroll" Height="10" Minimum="0" Maximum="400" ViewportSize="100"/>
            </Grid>
            </Grid>

            <!-- Separator -->

            <!-- Right Column: Settings & Options -->
            <Border Grid.Column="2" Margin="10,0,0,0">
                <ScrollViewer FlowDirection="RightToLeft" VerticalScrollBarVisibility="Auto">
                    <StackPanel FlowDirection="LeftToRight" Margin="0,0,9,0">
                        
                        <!-- Filter Card -->
                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/> <!-- Search Box -->
                                    <RowDefinition Height="Auto"/> <!-- Search Toggles -->
                                </Grid.RowDefinitions>
                                <!-- Search Row -->
                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Filter:" FontWeight="Bold" FontSize="12" Foreground="#999" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBox Grid.Column="1" Text="{Binding SearchFilter, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Padding="2" BorderBrush="#e0e0e0" BorderThickness="1"/>
                                    
                                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Center">
                                        <Button Content="Apply" Command="{Binding FilterTreeCommand}" Padding="12,3" Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                    </StackPanel>
                                </Grid>
             
                                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Left">
                                    <CheckBox Content="Use OR" IsChecked="{Binding FilterUseOr}" Style="{StaticResource SwitchStyle}" FontSize="11" Margin="0,0,10,0"/>
                                    <CheckBox Content="Only by name" IsChecked="{Binding FilterOnlyNames}" Style="{StaticResource SwitchStyle}" FontSize="11" Margin="0,0,10,0"/>
                                    <CheckBox Content="Use Regex" IsChecked="{Binding FilterUseRegex}" Style="{StaticResource SwitchStyle}" FontSize="11" Margin="0,0,10,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- On Duplicates Card -->
                        <Border Style="{StaticResource CardBorderStyle}" Padding="15,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="On Duplicates:" FontWeight="Bold" FontSize="12" Foreground="#999" VerticalAlignment="Center" Margin="0,0,15,0"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <RadioButton Content="Ok" IsChecked="{Binding OverrideDuplicates}" Margin="0,0,15,0"/>
                                    <RadioButton Content="Abort" IsChecked="{Binding CancelDuplicates}" Margin="0,0,15,0"/>
                                    <RadioButton Content="Ask User" IsChecked="{Binding AskDuplicates}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- On Views & Geometry Card -->
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="On Views:" FontWeight="Bold" FontSize="12" Foreground="#999" Margin="0,0,0,8"/>
                                
                                <CheckBox Content="Accept on all Dialogs" IsChecked="{Binding AcceptAllWarnings}" Style="{StaticResource SwitchStyle}" Margin="0,4"/>
                                
                                <CheckBox Content="Transfer Sheet with Views" IsChecked="{Binding IncludeSheetsWithViews}" Style="{StaticResource SwitchStyle}" Margin="0,4"/>
                                <StackPanel Margin="20,2,0,6">
                                    <CheckBox Content="Use Legend if Exists in Target" IsChecked="{Binding UseLegendIfExists}" Style="{StaticResource SwitchStyle}" Margin="0,3"/>
                                    <CheckBox Content="Use Schedule if Exists in Target" IsChecked="{Binding UseScheduleIfExists}" Style="{StaticResource SwitchStyle}" Margin="0,3"/>
                                    <CheckBox Content="Use Assembly Views if Exists in Targ" IsChecked="{Binding UseAssemblyViewsIfExists}" Style="{StaticResource SwitchStyle}" Margin="0,3"/>
                                </StackPanel>
                                
                                <CheckBox Content="Transfer Callouts of Views" IsChecked="{Binding IncludeCallouts}" Style="{StaticResource SwitchStyle}" Margin="0,4"/>
                                <CheckBox Content="Transfer View Elements (Ask User)" IsChecked="{Binding IncludeViewElements}" Style="{StaticResource SwitchStyle}" Margin="0,4"/>
                                <CheckBox Content="Force Level in Level Base Views (Ask User)" IsChecked="{Binding ForceLevelInLevelBaseViews}" Style="{StaticResource SwitchStyle}" Margin="0,4"/>
                                
                                <Border BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Margin="0,10,0,10"/>
                                
                                <TextBlock Text="Transform By:" FontWeight="Bold" FontSize="12" Foreground="#999" Margin="0,0,0,8"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <RadioButton Content="None" IsChecked="{Binding TransformNone}" Margin="0,0,15,0" ToolTipService.InitialShowDelay="600" ToolTip="Do not apply coordinate transformations.&#x0a;&#x0a;Use when linking directly."/>
                                    <RadioButton Content="Link" IsChecked="{Binding TransformLink}" Margin="0,0,15,0" ToolTipService.InitialShowDelay="600" ToolTip="Apply the geometry transformation of the linked instance.&#x0a;&#x0a;Use when transferring from a loaded link."/>
                                    <RadioButton Content="Shared" IsChecked="{Binding TransformShared}" ToolTipService.InitialShowDelay="600" ToolTip="Apply Shared Coordinates transformation.&#x0a;&#x0a;Use when transferring to a different model sharing site coordinates."/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <!-- Rename Card -->
                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="Rename:" FontWeight="Bold" FontSize="12" Foreground="#999" Margin="0,0,0,8"/>
                                <Button Content="Apply" Command="{Binding OpenRenamePanelCommand}" Margin="2" Padding="6">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="#007ACC"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Style.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Style.Resources>
                                            <Style.Triggers>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#F3F3F3"/>
                                                    <Setter Property="Foreground" Value="#B0B0B0"/>
                                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Bottom Row: Filter and Command Buttons -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Bottom Left: Elements Checked Card & Config -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom">
                <Button Command="{Binding OpenConfigurationCommand}" Margin="0,0,10,0" VerticalAlignment="Bottom" Cursor="Hand" ToolTip="Configuration">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="Transparent" CornerRadius="12" Padding="4">
                                <Viewbox Width="18" Height="18">
                                    <Path Data="M12,15.5A3.5,3.5 0 0,1 8.5,12A3.5,3.5 0 0,1 12,8.5A3.5,3.5 0 0,1 15.5,12A3.5,3.5 0 0,1 12,15.5M19.43,12.97C19.47,12.65 19.5,12.33 19.5,12C19.5,11.67 19.47,11.34 19.43,11L21.54,9.37C21.73,9.22 21.78,8.95 21.66,8.73L19.66,5.27C19.54,5.05 19.27,4.97 19.05,5.05L16.56,6.05C16.04,5.66 15.47,5.34 14.86,5.12L14.47,2.5C14.43,2.25 14.22,2.07 13.97,2.07H9.97C9.72,2.07 9.5,2.25 9.47,2.5L9.09,5.12C8.47,5.34 7.9,5.66 7.38,6.05L4.89,5.05C4.67,4.97 4.4,5.05 4.27,5.27L2.27,8.73C2.15,8.95 2.2,9.22 2.39,9.37L4.5,11C4.47,11.34 4.45,11.67 4.45,12C4.45,12.33 4.47,12.65 4.5,12.97L2.39,14.63C2.2,14.78 2.15,15.05 2.27,15.27L4.27,18.73C4.4,18.95 4.67,19.03 4.89,18.95L7.38,17.95C7.9,18.34 8.47,18.66 9.09,18.88L9.47,21.5C9.5,21.75 9.72,21.93 9.97,21.93H13.97C14.22,21.93 14.43,21.75 14.47,21.5L14.86,18.88C15.47,18.66 16.04,18.34 16.56,17.95L19.05,18.95C19.27,19.03 19.54,18.95 19.66,18.73L21.66,15.27C21.78,15.05 21.73,14.78 21.54,14.63L19.43,12.97Z" Fill="#777" x:Name="iconPath"/>
                                </Viewbox>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#eeeeee"/>
                                    <Setter TargetName="iconPath" Property="Fill" Value="#333"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#dddddd"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <Border Background="#ffffff" CornerRadius="8" Padding="15,5" BorderBrush="#e0e0e0" BorderThickness="1" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding CheckedElementsCount}" FontSize="24" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#333"/>
                        <TextBlock Text="Elements Checked" VerticalAlignment="Center" Foreground="#666" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Bottom Right: Execution Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button Content="Select" Width="90" Height="30" Margin="0,0,5,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#555555"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Style.Resources>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#666666"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="View Log" Width="90" Height="30" Margin="0,0,5,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#555555"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Style.Resources>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#666666"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Clear" Command="{Binding ClearFilterCommand}" Width="80" Height="30" Margin="0,0,5,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#E1E1E1"/>
                            <Setter Property="BorderBrush" Value="#707070"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Style.Resources>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#F3F3F3"/>
                                    <Setter Property="Foreground" Value="#B0B0B0"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Transfer Now" Command="{Binding TransferCommand}" Width="120" Height="30" FontWeight="Bold" BorderThickness="0" Margin="0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#007ACC"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Style.Resources>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#F3F3F3"/>
                                    <Setter Property="Foreground" Value="#B0B0B0"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
        </Grid>
        
        <!-- PowerRename Panel (Sidebar) -->
        <Grid Grid.Column="1" x:Name="ColRenameParent">
            <Border x:Name="RenamePanel" ClipToBounds="True" Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0"
                    HorizontalAlignment="Left" Width="{Binding ElementName=ColRenameParent, Path=ActualWidth}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="MaxWidth" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRenamePanelOpen}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="MaxWidth" To="2000" Duration="0:0:0.3">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="MaxWidth" To="0" Duration="0:0:0.3">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                
                <Grid Margin="15" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Body (Controls and Previews) -->
                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Search Controls (aligned with Apply transfer from ComboBox) -->
                    <StackPanel Grid.Row="0" Margin="0,21,0,15">
                        <!-- Find Input -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <TextBox x:Name="FindTextBox" Text="{Binding RenameSearchText, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" Height="32" VerticalContentAlignment="Center"/>
                                <TextBlock Text="Find" Foreground="#aaa" Margin="10,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=FindTextBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                            
                            <!-- RegEx Helper Button -->
                            <ToggleButton x:Name="BtnRegexHelper" Grid.Column="1" Width="20" Height="20" Margin="5,0,0,0" Cursor="Hand" ToolTip="Ayuda de RegEx">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border CornerRadius="10" Background="#F3F3F3" BorderThickness="1" BorderBrush="#E0E0E0">
                                            <TextBlock Text="?" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555" FontWeight="SemiBold" FontSize="10"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <Popup IsOpen="{Binding IsChecked, ElementName=BtnRegexHelper, Mode=TwoWay}" StaysOpen="False" PlacementTarget="{Binding ElementName=BtnRegexHelper}" Placement="Bottom" AllowsTransparency="True" Margin="0,5,0,0">
                                <Border Background="White" BorderBrush="#ccc" BorderThickness="1" CornerRadius="4" Padding="10" Width="380" MaxHeight="400">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                                    </Border.Effect>
                                    <StackPanel>
                                        <TextBlock Text="Ayuda de RegEx" FontWeight="Bold" Margin="0,0,0,15" FontSize="14"/>
                                        <ScrollViewer MaxHeight="330" VerticalScrollBarVisibility="Auto">
                                            <StackPanel>
                                                <StackPanel.Resources>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                        <Setter Property="Padding" Value="5"/>
                                                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                                        <Setter Property="Cursor" Value="Hand"/>
                                                    </Style>
                                                </StackPanel.Resources>
                                                
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="^" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="^" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Inicio de la cadena" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="$" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="$" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Fin de la cadena" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="." Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="." HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Coincide con cualquier carácter" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="+" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="+" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Coincide con uno o varios" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="?" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="?" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Cero o una coincidencia" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="*" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="*" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Cero o más coincidencias" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="\d" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="\d" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Cualquier dígito [0-9]" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="\D" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="\D" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Cualquier no-dígito" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertRegexHelperCommand}" CommandParameter="\w" Click="CloseRegexPopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="35" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="\w" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Carácter de palabra" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </Grid>

                        <!-- Checkboxes -->
                        <StackPanel Margin="0,0,0,15">
                            <CheckBox Content="Use regular expressions" IsChecked="{Binding RenameUseRegex}" Margin="0,0,0,6"/>
                            <CheckBox Content="Match all occurrences" IsChecked="{Binding RenameMatchAllOccurrences}" Margin="0,0,0,6"/>
                            <CheckBox Content="Match case" IsChecked="{Binding RenameMatchCase}"/>
                        </StackPanel>

                        <!-- Replace Input -->
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid>
                                <TextBox x:Name="ReplaceTextBox" Text="{Binding RenameReplaceText, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" Height="32" VerticalContentAlignment="Center"/>
                                <TextBlock Text="Changed by:" Foreground="#aaa" Margin="10,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=ReplaceTextBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                            
                            <!-- Date Helper Button -->
                            <ToggleButton x:Name="BtnDateHelper" Grid.Column="1" Width="20" Height="20" Margin="5,0,0,0" Cursor="Hand" ToolTip="Ayuda de Formato de Fecha">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border CornerRadius="10" Background="#F3F3F3" BorderThickness="1" BorderBrush="#E0E0E0">
                                            <TextBlock Text="?" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#555" FontWeight="SemiBold" FontSize="10"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            
                            <Popup IsOpen="{Binding IsChecked, ElementName=BtnDateHelper, Mode=TwoWay}" StaysOpen="False" PlacementTarget="{Binding ElementName=BtnDateHelper}" Placement="Bottom" AllowsTransparency="True" Margin="0,5,0,0">
                                <Border Background="White" BorderBrush="#ccc" BorderThickness="1" CornerRadius="4" Padding="10" Width="400" MaxHeight="400">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                                    </Border.Effect>
                                    <StackPanel>
                                        <TextBlock Text="Reemplazar por fecha/hora" FontWeight="Bold" Margin="0,0,0,15" FontSize="14"/>
                                        <ScrollViewer MaxHeight="330" VerticalScrollBarVisibility="Auto">
                                            <StackPanel>
                                                <StackPanel.Resources>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                        <Setter Property="Padding" Value="5"/>
                                                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                                        <Setter Property="Cursor" Value="Hand"/>
                                                    </Style>
                                                </StackPanel.Resources>
                                                
                                                <Button Command="{Binding InsertDateHelperCommand}" CommandParameter="$YYYY" Click="CloseDatePopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="60" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="$YYYY" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Año (4 dígitos)" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertDateHelperCommand}" CommandParameter="$YY" Click="CloseDatePopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="60" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="$YY" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Año (2 dígitos)" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertDateHelperCommand}" CommandParameter="$MM" Click="CloseDatePopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="60" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="$MM" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Mes (2 dígitos)" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertDateHelperCommand}" CommandParameter="$DD" Click="CloseDatePopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="60" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="$DD" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Día del mes (2 dígitos)" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertDateHelperCommand}" CommandParameter="$hh" Click="CloseDatePopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="60" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="$hh" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Horas (24h)" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                                <Button Command="{Binding InsertDateHelperCommand}" CommandParameter="$mm" Click="CloseDatePopup">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Width="60" Height="35" Margin="0,0,10,0">
                                                            <TextBlock Text="$mm" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                        </Border>
                                                        <TextBlock Text="Minutos" VerticalAlignment="Center" Foreground="#555"/>
                                                    </StackPanel>
                                                </Button>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </Grid>

                        <!-- Text Format -->
                        <TextBlock Text="Text format:" Margin="0,0,0,8" Foreground="#555" FontSize="12"/>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel.Resources>
                                <Style TargetType="ToggleButton">
                                    <Setter Property="Foreground" Value="#333"/>
                                    <Setter Property="Background" Value="#F3F3F3"/>
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Setter Property="Margin" Value="0,0,4,0"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border CornerRadius="4" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter Property="Background" Value="#007ACC"/>
                                            <Setter Property="BorderBrush" Value="#007ACC"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#EBEBEB"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsChecked" Value="True"/>
                                                <Condition Property="IsMouseOver" Value="True"/>
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Background" Value="#005A9E"/>
                                        </MultiTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Resources>
                            <!-- ToggleButtons for Casing -->
                            <ToggleButton Content="aa" ToolTip="Lowercase" IsChecked="{Binding IsFormatLowercase, Mode=TwoWay}" Width="36" Height="32"/>
                            <ToggleButton Content="AA" ToolTip="Uppercase" IsChecked="{Binding IsFormatUppercase, Mode=TwoWay}" Width="36" Height="32"/>
                            <ToggleButton Content="Aa" ToolTip="Title Case" IsChecked="{Binding IsFormatTitleCase, Mode=TwoWay}" Width="36" Height="32"/>
                            <ToggleButton Content="Aa Aa" ToolTip="Capitalize Each Word" IsChecked="{Binding IsFormatCapitalizeEach, Mode=TwoWay}" Width="52" Height="32" Margin="0,0,15,0"/>
                            
                            <!-- ToggleButtons for Enumeration and Random -->
                            <ToggleButton Content="123" ToolTip="Enumerate Items" IsChecked="{Binding RenameEnumerateItems, Mode=TwoWay}" Width="36" Height="32"/>
                            <ToggleButton Content="🔀" ToolTip="Randomize Items" IsChecked="{Binding RenameRandomizeItems, Mode=TwoWay}" Width="36" Height="32"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Preview DataGrid -->
                    <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="6" Padding="1">
                        <DataGrid ItemsSource="{Binding RenamePreviewItems}" 
                                  AutoGenerateColumns="False" 
                                  CanUserAddRows="False" 
                                  IsReadOnly="False"
                                  HeadersVisibility="Column"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#f0f0f0">
                            <DataGrid.Columns>
                                <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected}" Width="30"/>
                                <DataGridTextColumn Header="Original" Binding="{Binding OriginalName}" Width="*" IsReadOnly="True"/>
                                <DataGridTextColumn Header="New Name" Binding="{Binding NewName}" Width="*" IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="#007ACC"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>

                <!-- Actions Footer -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button Content="Cancel" Command="{Binding CloseRenamePanelCommand}" Width="100" Height="30">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#555555"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Style.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="4"/>
                                    </Style>
                                </Style.Resources>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#666666"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>

        <!-- Busy Overlay -->
        <Grid Grid.ColumnSpan="2" Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <Border Background="White" CornerRadius="10" Padding="25" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="1" BorderBrush="#ddd">
                <StackPanel>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <ProgressBar Value="{Binding ProgressPercentage}" IsIndeterminate="{Binding ProgressPercentage, Converter={x:Null}}" Width="250" Height="8"/>
                    <TextBlock Text="{Binding ProgressPercentage, StringFormat={}{0}%}" HorizontalAlignment="Center" Margin="0,5,0,0" FontSize="11" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>